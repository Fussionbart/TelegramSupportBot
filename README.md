# TelegramSupportBot
🚀 Telegram Support Bot

Telegram-бот для обработки тикетов и общения с пользователями

Этот бот — полностью автоматизированная система поддержки, построенная вокруг тикетов.
Он собирает сообщения пользователей, группирует их в запросы, уведомляет администратора,
и позволяет удобно вести диалог и закрывать тикеты.

⸻

✨ Возможности

📩 Создание тикетов
 • Пользователь пишет в бота — создаётся тикет с уникальным ID вида NR-XXXXXX.
 • Все сообщения пользователя добавляются в историю до тех пор, пока тикет не закрыт.

⏳ Умная задержка (ACK_DELAY)
 • Бот ждёт, пока пользователь перестанет писать (по умолчанию 90 секунд).
 • После паузы отправляет пользователю подтверждение.
 • Одновременно отправляет уведомление администратору.

🔔 Умные уведомления администратору
 • Если тикет новый → сообщение: 📨 Новый запрос
 • Если пользователь дописал в уже существующий тикет → 💬 Пользователь снова написал
 • Уведомление приходит только один раз за цикл.

🧑‍💻 Панель администратора
 • В админ-панели показываются все открытые тикеты.
 • Можно открыть тикет, посмотреть последние сообщения, ответить или закрыть.
 • Поддерживается переход в личку пользователя через кнопку.

🗂 Управление UI администратора
 • Уведомления, панели и карточки тикетов автоматически удаляются, чтобы не засорять чат.
 • Для админа есть постоянная нижняя клавиатура с кнопкой «Панель».

✔ Автоматическое закрытие и повторное открытие тикетов
 • После закрытия тикета пользователь может просто написать снова — создастся новый тикет.
 • Ведётся чистый учёт пользователей и их состояний.

⸻

🛠 Технологии
 • Python 3.10+
 • Aiogram 3
 • JSON-хранилище для простоты деплоя
 • Асинхронная архитектура без блокировок
 • Чистая и детальная структура модулей:
 • handlers/ — админ и юзер
 • services/ — логика работы с тикетами, пользователями, scheduler
 • core/ — утилиты, загрузчик, диспетчер
 • data/ — файлы тикетов и сообщений
